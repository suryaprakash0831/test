# Brand assets

Place the official, unmodified Blue Yonder logo at:

```text
assets/blue-yonder-logo.svg
```

The UI references that file directly and does not transform, redraw, or edit the logo. If your official asset is a PNG, place it in this directory and update the `src` value in `index.html` to the exact file name.
